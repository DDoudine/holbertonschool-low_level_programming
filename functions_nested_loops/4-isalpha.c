#include <stdio.h>
#include "main.h"
#include <ctype.h>

/* more headers goes there */

/**
 * _isalpha - Entry
 * @c: character
 *
 * Return: 0
 */

int _isalpha(int c)
{
	if (c >= 'a' && c <= 'z')
	{

	}
	else
	{
		return (0);
	}

	if (c >= 'A' && c <= 'Z')
        {

        }
        else
        {
                return (0);
        }

}
